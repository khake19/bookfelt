import { Text } from 'react-native';
import { ScreenWrapper } from '../../shared';

export default function SearchScreen() {
  return (
    <ScreenWrapper>
      <Text className="text-foreground text-2xl font-bold">Search</Text>
    </ScreenWrapper>
  );
}
